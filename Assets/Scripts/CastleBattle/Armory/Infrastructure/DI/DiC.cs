using UnityEngine;
using System;
using System.Collections.Generic;

namespace CastleBattle.Armory
{
	public class DiC : MonoBehaviour
	{
		/*private IDictionary<Type, object> instances;
		//const Type WEAPONREPOSITORY = typeof(WeaponRepository);

		// Use this for initialization
		void Start ()
		{
			instances = new SortedList<string, object> ();
			//instances.Add (WEAPONREPOSITORY, new WeaponRepositoryImp ());
		}
	
		// Update is called once per frame
		void Update ()
		{
	
		}

		private object GetInstanceOf(string className)
		{
			object o;
			instances.TryGetValue (className, out o);
			return o;
		}

		public WeaponRepository BuildWeaponRepository() 
		{
			//return GetInstanceOf (WEAPONREPOSITORY);
		}*/
	}
}
